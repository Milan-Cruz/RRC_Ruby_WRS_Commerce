<% # Header %>
<%= render "shared/header" %>

<% # Hero Section %>
<section class="hero bg-primary text-white text-center py-5">
  <div class="container">
    <h1 class="display-4 text-white">Find Your Dream Car</h1>
    <p class="lead">Explore a wide range of vehicles tailored to your preferences.</p>
    <a href="/cars" class="btn btn-light btn-lg">Browse Listings</a>
  </div>
</section>

<% # Featured Categories %>
<section class="categories py-5">
  <div class="container">
    <h2 class="text-center mb-4">Popular Categories</h2>
    <div class="row">
    <% # render partial: "categories/category_card", collection: @categories, as: :category %>
      <%= render partial: "categories/category_card", collection: @categories.select { |c| ["SUVs", "Sedans", "Trucks"].include?(c.name) }, as: :category %>
    </div>
  </div>
</section>

<% # Featured Listings %>
<section class="listings py-5 bg-light">
  <div class="container">
    <h2 class="text-center mb-4">Featured Cars</h2>
    <div class="row">
      <% 3.times do |i| %>
        <div class="col-md-4">
          <div class="card">
            <img src="https://via.placeholder.com/350x150" class="card-img-top" alt="Car Image">
            <div class="card-body">
              <h5 class="card-title">Car Make and Model <%= i + 1 %></h5>
              <p class="card-text">Price: $<%= (i + 1) * 10_000 %></p>
              <a href="/cars/<%= i + 1 %>" class="btn btn-primary">View Details</a>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>

<% # Footer %>
<%= render "shared/footer" %>
